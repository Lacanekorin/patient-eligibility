services:
  patient-checker:
    build:
      context: .
      dockerfile: Dockerfile
    image: patient-eligibility-checker:latest
    container_name: eligibility-checker
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./models:/root/.cache/huggingface/hub
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    restart: unless-stopped
